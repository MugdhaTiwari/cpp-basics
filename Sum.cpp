#include <iostream>
using namespace std;

int main()
{
    int x, y, z;

    // Asking the user to give input
    cout << "Enter three integers: ";
    cin >> x >> y >> z;

    // Printing the sum using arithmetic (+) operator
    cout << "Sum = " << x + y + z << endl;

    return 0;
}